// import { NextResponse } from 'next/server';

// const getUser = async (request: Request, { params }) => {
//   //   const account_no = +params.id;
//   try {
//     return new NextResponse(JSON.stringify(params), { status: 201 });
//   } catch (err) {
//     return new NextResponse(
//       JSON.stringify({ err, message: 'User not found' }),
//       { status: 500 }
//     );
//   }
// };

// export { getUser as GET };
